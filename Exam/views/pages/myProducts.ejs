<%- include('../partials/header', { title: 'My Products', user: user }) %>
  <h2>My Products</h2>
  <div class="grid">
    <% products.forEach(function(p){ %>
      <div class="card">
        <h3><%= p.title %></h3>
        <p><%= p.description %></p>
        <p>Price: ₹<%= p.price %></p>
        <a href="/products/edit/<%= p._id %>">Edit</a>
        <form method="POST" action="/products/delete/<%= p._id %>" style="display:inline">
          <button type="submit">Delete</button>
        </form>
      </div>
    <% }) %>
  </div>
<%- include('../partials/footer') %>
